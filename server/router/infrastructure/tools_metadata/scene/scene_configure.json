{
  "tool_name": "scene_configure",
  "category": "scene",
  "mode_required": "ANY",
  "selection_required": false,
  "keywords": ["configure", "render", "world", "background", "color management", "scene settings", "node graph handoff"],
  "sample_prompts": [
    "set the render engine to cycles",
    "configure scene color management for AgX",
    "set the world background strength to 0.8",
    "apply scene render settings from this payload"
  ],
  "parameters": {
    "action": {
      "type": "enum",
      "options": ["render", "color_management", "world"],
      "required": true
    },
    "settings": {
      "type": "object",
      "required": true
    }
  },
  "related_tools": ["scene_inspect", "scene_get_viewport", "node_graph_inspect", "node_graph_build"],
  "patterns": [],
  "description": "Applies grouped render, color-management, or bounded world/background settings from structured input, while keeping full node-graph rebuilds outside this tool."
}
